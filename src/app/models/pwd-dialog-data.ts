export class PwdDialogData {
  
    constructor(id: number, title: string, folderId: number ) {
      this.id = id;
      this.title = title;
      this.folderId = folderId;
    }

    id: number;
    title: string;
    folderId: number;
  }
  